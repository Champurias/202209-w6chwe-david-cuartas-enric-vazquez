import { RobotsState } from "../redux/features/types";
import { mockRobotsList } from "./mocksRobots";

const mockRobotState: RobotsState = {
  robotsList: mockRobotsList,
};

export default mockRobotState;
